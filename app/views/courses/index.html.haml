-content_for :description, 'Course Listings'
-content_for :title, 'Courses'
.container
  .row
    .h1 Courses
    =link_to 'add', new_course_path
    .column
      %table.table
        %tbody
          -@courses.each do |course|
            %tr.course= render course