- if object.errors.any?
  .error-messages
    %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", type: "button"}
      \&#215;
    %strong
      #{pluralize(object.errors.count, "error").titleize}:
    %ul
      - object.errors.full_messages.each do |msg|
        %li= msg
