%li= link_to "Courses", courses_path
- if user_signed_in? && current_user.admin?
  %li= link_to "Users", users_path
- if user_signed_in?
  - if @active_plan
    %li= link_to "Planning", plan_path(@active_plan)
  - else
    %li= link_to "Planning", user_plans_path(current_user)

