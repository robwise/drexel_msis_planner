- content_for :title, 'Edit Plan'
- content_for :description, 'Form for editing a plan.'
%h1 Editing plan

= render 'form', plan_decorator: @plan
= link_to 'Show', plan_path(@plan.id)
\|
= link_to 'Back', user_plans_path(@plan.user)
